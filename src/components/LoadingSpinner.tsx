const LoadingSpinner = () => (
  <div className="flex h-32 items-center justify-center">
    <div className="size-16 animate-spin rounded-full border-b-2 border-gray-800"></div>
  </div>
);

export default LoadingSpinner;
